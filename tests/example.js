define( 'build/app/example', function( Example ) {

  suite('example', function() {

    test('Example exists', function() {
    
      var testExample = new Example();
      asert(textExample, 'Example created';
    
    });

  });

});